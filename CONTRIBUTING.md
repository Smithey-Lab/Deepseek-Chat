# Contributing

Branch from `dev`, keep changes focused, and submit pull requests to `dev`. Run `npm ci`, `npm run check`, and `npm run dist`. Explain resulting behavior and validation in your pull request. Never commit credentials or local conversations.

Promote `dev` to `main` through a separate pull request after Codex review. Releases are dispatched from `main` using the documented release workflow.
