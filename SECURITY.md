# Security

Do not include API keys, tokens, private repository content, or chat history in public issues. Report vulnerabilities using this repository's private security reporting feature when enabled. Rotate any exposed provider credentials immediately.

Only the latest release is supported. The first milestone uses encrypted local credentials, sandboxed rendering, and user-confirmed dev-only writes. It does not run AI-generated commands. Release builds currently have no publisher signing certificate configured.
